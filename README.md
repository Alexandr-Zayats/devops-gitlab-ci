# GitLab CI building blocks

Reusable CI templates for build, test, release, deployment and notification workflows.

All credentials and service endpoints are expected through protected CI/CD variables. The checked-in defaults are examples and must be reviewed before reuse.
