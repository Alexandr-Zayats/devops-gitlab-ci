# Security policy

Use protected and masked CI/CD variables for tokens, webhook credentials, cloud credentials and signing keys. Do not place secrets in YAML comments: comments are still published history.
